import { getUnix } from './unix';
import { bmi, reverseBMI, bmiStatus, hrStatus, hrTarget, calculateAge } from './bmi';

export { bmi, reverseBMI, bmiStatus, hrStatus, hrTarget, calculateAge, getUnix };
